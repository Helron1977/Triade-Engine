import { defineConfig } from 'vite';

export default defineConfig({
    base: './', // Use relative paths for Github Pages
    build: {
        outDir: '../docs',
        emptyOutDir: true,
    }
});





































